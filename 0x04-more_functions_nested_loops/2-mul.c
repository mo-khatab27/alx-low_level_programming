#include "main.h"

/**
 * mul - function to multiply two numbers
 * @a: the first number
 * @b: the second number
 * Return: the value of multiplycation
 */

int mul(int a, int b)
{
	return (a * b);
}
